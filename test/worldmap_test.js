/* global describe it beforeEach expect afterEach */

import Thredds from '../src/thredds';
import DataBuilder from './data_builder';

// NEED TESTS

describe('Worldmap', () => {
  let worldMap;
  let ctrl;
});
